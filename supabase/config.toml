# Minimal Supabase CLI project config.
# Used only if you install the Supabase CLI locally or run the GitHub Action
# in .github/workflows/deploy-db.yml. Not required for the manual "paste into
# SQL Editor" setup path described in SUPABASE_SETUP.md.
project_id = "nsxvasvceslhhvgjkedh"
